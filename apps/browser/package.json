{
  "name": "@ts/browser",
  "type": "module",
  "scripts": {
    "clean": "npx tsc --build --clean",
    "build": "npx tsc --build && npx vite build",
    "start": "node ./dist/index.js"
  },
  "dependencies": {
    "@ts/p2": "workspace:*"
  }
}
