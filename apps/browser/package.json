{
  "name": "@gpk/browser",
  "type": "module",
  "scripts": {
    "clean": "npx tsc --build --clean",
    "build": "npx tsc --build && npx lerna run postbuild && npx vite build",
    "start": "node ./dist/index.js"
  },
  "dependencies": {
    "@gpk/console": "workspace:*",
    "@gpk/core": "workspace:*"
  }
}
